import {
  Container,
} from '@components'
import { useTheme } from '@themes'
import React from 'react'
//en_US
export const Home = () => {
  const { colors } = useTheme()

  return (
    <Container >

    </Container>
  )
}
